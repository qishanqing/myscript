qos:
    kernel: v3
    setting:
        type: server
        port: 20001
    filter:
        type: white
        topics:
            - /data

plugins:
    - {name: publisher, lib: libpublisher_plugin.so}
#    - {name: subcriber, lib: libsubscriber_plugin.so}
