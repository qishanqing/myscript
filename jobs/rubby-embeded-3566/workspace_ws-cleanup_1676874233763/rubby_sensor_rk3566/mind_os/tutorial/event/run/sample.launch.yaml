qos:
    kernel: v3

nodes:
  - node_name: pub
    plugins:
      - {plugin_name: publisher, lib: libpublisher_plugin.so, method: Create}
  - node_name: sub
    plugins:
      - {plugin_name: subscriber, lib: libsubscriber_plugin.so, method: Create}
