qos:
    kernel: v3
    setting:
        type: client
        port: 20001
        threads: 4

plugins:
    - {name: client1, lib: libclient_plugin.so}
#    - {name: client2, lib: libclient_plugin.so}
#    - {name: server, lib: libserver_plugin.so}
