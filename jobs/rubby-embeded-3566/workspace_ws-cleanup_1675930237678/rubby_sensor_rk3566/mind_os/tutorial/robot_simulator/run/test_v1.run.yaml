qos:
    kernel: v1

plugins:
    - {name: system, lib: libsys_plugin.so}
    - {name: sensor, lib: libsensor_plugin.so}
    - {name: slam, lib: libslam_plugin.so}
    - {name: nav, lib: libnav_plugin.so}
    - {name: hmi, lib: libhmi_plugin.so}
